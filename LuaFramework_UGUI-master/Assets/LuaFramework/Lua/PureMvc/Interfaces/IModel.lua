local IModel = Class("IModel", CONST.ClassType.CORE)

function IModel:RegisterProxy(proxy)
end

function IModel:RetrieveProxy(proxyName)
end

function IModel:RemoveProxy(proxyName)
end

function IModel:HasProxy(proxyName)
end

return IModel